@Html.Bootstrap().Heading1("Simple Container")
@using (Html.Bootstrap().Container().Begin())
{
    <p>
        Containers are the most basic layout element in Bootstrap and are required when using our default grid system.
        Containers are used to contain, pad, and (sometimes) center the content within them. While containers can be nested, most layouts do not require a nested container.
    </p>
}
<hr />
@Html.Bootstrap().Heading1("Fluid Container")
@using (Html.Bootstrap().Container().SetFluid().Begin())
{
    <p>
        Containers are the most basic layout element in Bootstrap and are required when using our default grid system.
        Containers are used to contain, pad, and (sometimes) center the content within them. While containers can be nested, most layouts do not require a nested container.
    </p>
}
<hr />
@Html.Bootstrap().Heading1("Responsive Container")
@using (Html.Bootstrap().Container().SetResponsive(ContainerSize.Small).Begin())
{
    @Html.Bootstrap().Icon(IconType.Pencil,"pencil")
    <p>
        Containers are the most basic layout element in Bootstrap and are required when using our default grid system.
        Containers are used to contain, pad, and (sometimes) center the content within them. While containers can be nested, most layouts do not require a nested container.
    </p>
}
<hr />
